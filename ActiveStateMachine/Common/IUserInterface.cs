namespace Common
{
    /// <summary>
    /// Interface UIs needed to implement to work with ViewManager
    /// </summary>
    public interface IUserInterface
    {
        void LoadViewState(string viewState);
    }
}
